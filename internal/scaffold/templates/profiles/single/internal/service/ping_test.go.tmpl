package service

import (
	"context"
	"testing"
)

func TestPingEcho(t *testing.T) {
	var reply PingReply
	if err := NewPing().Echo(context.Background(), &PingRequest{}, &reply); err != nil {
		t.Fatal(err)
	}
	if reply.Message != "pong" {
		t.Fatalf("message = %q, want pong", reply.Message)
	}
}
