# {{.Module}}

Generated by `tgfctl` using profile `{{.Profile}}`, data mode `{{.Data}}`, protocol `{{.Protocol}}`, and tgf `{{.TGFVersion}}`.

## Local checks

```sh
go mod tidy
go build ./...
go vet ./...
go test ./...
tgfctl verify --dir .
```

`.env.example` contains no credentials. `.env.dev` is ignored and is for local development only. Production credentials must come from the deployment secret manager.
{{if .UseRedis}}
The generated `internal/dao` package contains the selected Redis data-layer starting point.
{{end}}{{if .UseMySQL}}
Wire `dao.ReplayUserFailures` during application startup with the project-specific durable flush callback; the generated cache already includes memory, Redis, write-behind batching, retry, and a failure queue.
{{end}}
{{if .Distributed}}
Run the processes separately with `go run ./cmd/game` and `go run ./cmd/gateway` after Redis and Consul are available.
{{else}}
Run with `go run .` after required data services are available.
{{end}}
{{if eq .Deploy "compose"}}
For local dependencies and containers, export the required secret variables and run `docker compose up --build`.
{{end}}{{if eq .Deploy "k8s"}}
Before applying `k8s.yaml`, replace the `replace-me/*` images, provide the referenced Secret keys, and point Redis/Consul/MySQL service names at your cluster services.
{{end}}
